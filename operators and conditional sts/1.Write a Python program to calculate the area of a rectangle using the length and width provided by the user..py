#Write a Python program to calculate the area of a rectangle using the length and width provided by the user.

length = float(input("enter the number:"))
width = float(input("enter the number:"))

area = length * width
print(area)