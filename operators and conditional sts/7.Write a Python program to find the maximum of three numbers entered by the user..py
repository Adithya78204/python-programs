#7.Write a Python program to find the maximum of three numbers entered by the user.
numbers = []

for i in numbers(3):
    num = float(input("enter the nubers:"))
    numbers.append()
max_numbers = [0]

for num in numbers:

    
    if num > max_numbers:
        max_numbers = num

print(numbers)
